{{!-- Blog Section --}}
<div class="bg-gray-100 p-4 h-screen">
  <form class="mb-4 mt-10 w-full sm:w-3/4 md:w-2/3 lg:w-1/2 xl:w-1/3">
    <h2 class="text-2xl sm:text-3xl font-extrabold">blog.addEventListener('submit', newPost);</h2>
    <p class="text-sm sm:text-base md:text-lg">Submit a story, good or bad, about your coding journey.</p>
    <label for="title" class="block mb-2 font-bold text-gray-700 mt-4">Title:</label>
    <input type="text" id="title" name="title"
      class="w-full px-3 py-2 mb-3 leading-tight text-gray-700 border rounded shadow appearance-none focus:outline-none focus:shadow-outline">
    <label for="content" class="block mb-2 font-bold text-gray-700">Content:</label>
    <textarea id="content" name="content"
      class="w-full px-3 py-2 mb-3 leading-tight text-gray-700 border rounded shadow appearance-none focus:outline-none focus:shadow-outline"></textarea>
    <button type="submit"
      class="px-4 py-2 font-bold text-white bg-blue-900 rounded hover:bg-blue-800 focus:outline-none focus:shadow-outline-blue">Submit</button>
  </form>
  <hr class="my-8">
  <div class="recent-posts p-4 mt-10">
    <h3 class="text-2xl sm:text-3xl font-extrabold">let blog = [{recent.userPosts}];</h3>
    <p class="text-sm sm:text-base md:text-lg">Look at stories from other user to inspire, or relate.</p>
    {{#each blogPosts}}
    <div class="post bg-white p-4 mb-4 rounded shadow">
      <h2 class="text-xl sm:text-2xl font-bold mb-2">{{this.title}}</h2>
      <p class="text-gray-700">{{this.content}}</p>
    </div>
    {{/each}}
  </div>
</div>